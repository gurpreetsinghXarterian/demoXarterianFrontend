export const selectAllPosts = (state) => state.post.posts;
export const selectAllVideoPosts = (state) => state.post.allVideoPosts;
export const selectLoader = (state) => state.post.loading;
export const selectAllUserPosts = (state) => state.post.userPosts;
